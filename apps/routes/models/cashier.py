from apps.utilities.responseHelpers import bad_request


# CASHIER MODELS ============================================================ Begin
class CashierModels():

    # DASHBOARD ============================================================ Begin
    def dashboard():
        try:
            pass

        except Exception as e:
            return bad_request(str(e))
    # DASHBOARD ============================================================ End


    # TODAY SUMMARY ============================================================ Begin
    def today_summary():
        try:
            pass

        except Exception as e:
            return bad_request(str(e))
    # TODAY SUMMARY ============================================================ End


    # TRANSACTION HISTORY ============================================================ Begin
    def transaction_history():
        try:
            pass

        except Exception as e:
            return bad_request(str(e))
    # TRANSACTION HISTORY ============================================================ End


    # CASHIER REPORT ============================================================ Begin
    def cashier_report(start_date, end_date):
        try:
            pass

        except Exception as e:
            return bad_request(str(e))
    # CASHIER REPORT ============================================================ End


    # SALES SUMMARY ============================================================ Begin
    def sales_summary():
        try:
            pass

        except Exception as e:
            return bad_request(str(e))
    # SALES SUMMARY ============================================================ End


    # PRODUCT SUMMARY ============================================================ Begin
    def product_summary():
        try:
            pass

        except Exception as e:
            return bad_request(str(e))
    # PRODUCT SUMMARY ============================================================ End


    # SERVICE SUMMARY ============================================================ Begin
    def service_summary():
        try:
            pass

        except Exception as e:
            return bad_request(str(e))
    # SERVICE SUMMARY ============================================================ End


    # CUSTOMER HISTORY ============================================================ Begin
    def customer_history(customer_id):
        try:
            pass

        except Exception as e:
            return bad_request(str(e))
    # CUSTOMER HISTORY ============================================================ End


    # SALES RECEIPT ============================================================ Begin
    def sales_receipt(payment_id):
        try:
            pass

        except Exception as e:
            return bad_request(str(e))
    # SALES RECEIPT ============================================================ End


# CASHIER MODELS ============================================================ End